@model IEnumerable<FbcBookIt.Entity.BookRequest>

@{
    ViewBag.Title = "Book Requests";
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FormatTypeId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RequestDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RequestStatusId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StudentTeacherSchoolId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TitleID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FormatTypeId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RequestDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RequestStatusId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StudentTeacherSchoolId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TitleID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BookRequestId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BookRequestId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BookRequestId })
        </td>
    </tr>
}

</table>
