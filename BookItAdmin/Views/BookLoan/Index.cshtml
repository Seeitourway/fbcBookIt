@model IEnumerable<FbcBookIt.Entity.BookLoan>

@{
    ViewBag.Title = "Book Loans";
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BookRequestID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LoanNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.InDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OutDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LoanStatusID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VolumeID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BookRequestID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LoanNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.InDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OutDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LoanStatusID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VolumeID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BookLoanId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BookLoanId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BookLoanId })
        </td>
    </tr>
}

</table>
